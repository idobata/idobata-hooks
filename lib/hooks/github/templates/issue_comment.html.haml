%div
  %span= avatar_image_tag payload.sender.avatar_url
  %a{href: payload.sender.html_url}= payload.sender.login
  commented on #{payload.issue.pull_request? ? 'pull request' : 'issue'}
  %a{href: payload.comment.html_url} #{payload.repository.full_name}##{payload.issue.number}
  %b= payload.issue.title

= md payload.comment.body

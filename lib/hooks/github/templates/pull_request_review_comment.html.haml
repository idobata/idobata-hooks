%p
  %span= avatar_image_tag payload.sender.avatar_url
  %a{href: payload.sender.html_url}= payload.sender.login
  commented on pull request
  -# XXX We want to render pull request title, but payload doesn't contain own title...
  %a{href: payload.comment.html_url} #{payload.repository.full_name}##{payload.comment.pull_request_url[/\d+$/]}

= md payload.comment.body

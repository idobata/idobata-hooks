:ruby
  repository = payload.repository
  issue      = payload.issue
  actor      = payload.actor

%p
  %span
    = avatar_image_tag(actor.links.avatar.href)

  %a{href: actor.links.html.href}
    = actor.display_name
  created issue to
  %a{href: issue.links.html.href}
    #{repository.full_name}##{issue.id}

  %b= issue.title

.bitbucket-html!= issue.content.html
